# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.pytest_cache/
*.sock

# Project-local dedicated-instance markers (#80/#436): a `.bn-<id>` file dropped
# in the repo root so a bare `bn` resolves the right bridge. Instance IDs can
# encode target/campaign hints; never let a broad `git add` sweep one into VCS.
.bn-*

# Test fixtures: ignore built binaries + databases, keep src/*.c + Makefile (#167)
tests/fixtures/*_x86_64
tests/fixtures/*.bndb
tests/fixtures/*.bndb.*

# Local dogfood/review scratch artifacts (not part of the project)
DEEPSEEK_*.md
*VIBERSS*
RE-VIBE.json
vibe_check.py
vibe_report.json
.dogfood/
docs
# Root-level design/review/dogfood notes -- local only, never committed
AGENTS.md
CODEX_*.md
DOGFOOD_*.md
FORWARD_TAINT_DESIGN.md
IDEA_001.md
TESTING_PLAN_*.md
hermes.md
